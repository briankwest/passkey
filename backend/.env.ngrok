# Ngrok Development Configuration
DATABASE_URL=postgresql://postgres:password@localhost:5432/passkey_demo
SESSION_SECRET=ngrok-dev-session-secret
JWT_SECRET=ngrok-dev-jwt-secret
PORT=5000
NODE_ENV=development

# Update these with your ngrok domain
RP_NAME=Passkey Demo
RP_ID=localhost
ORIGIN=https://your-domain.ngrok.io

# For WebAuthn to work with ngrok, you might need to use localhost as RP_ID
# and handle the origin validation more flexibly